import { ProjectDetails } from '../types'
import { solids } from './artworks'

const projectsDetails: ProjectDetails[] = [
  {
    id: '1',
    invocations: 500,
    minted: 250,
    type: 'glb',
    artworks: solids
  }
]

export default projectsDetails
